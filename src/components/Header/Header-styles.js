import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
  },
  title: {
    color: "#212121",
    fontSize: 17,
    fontStyle: "normal",
    fontWeight: 500,
    lineHeight: 22,
    letterSpacing: -0.4,
  },
});
