import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  user__container: { flexDirection: "row", alignItems: "center" },
  avatar: { width: 60, height: 60, borderRadius: 16, marginRight: 8 },
});
